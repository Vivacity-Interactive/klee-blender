export enum NodeState {
    NONE = 0,
    MUTED = 1 << 0,
    HIDDEN = 1 << 1,
    ENABLED = 1 << 2,
    OPTIONS = 1 << 3,
    ADVANCED = 1 << 4,
    LATENT = 1 << 5,
    DEPRECATED = 1 << 6,
    SELECTED = 1 << 7,
    COLLAPSED = 1 << 8,
    DEFAULT = ENABLED
}

export enum NodeCategory {
    CONVERTER_NODE,
    TEXTURE_NODE,
    VECTOR_NODE,
    GROUP_SOCKET_NODE,
    GROUP_NODE,
    INPUT_NODE,
    OUTPUT_NODE,
    ATTRIBUTE_NODE,
    COLOR_NODE,
    FRAME_NODE,
    MATTE_NODE,
    DISTOR_NODE,
    FILTER_NODE,
    SHADER_NODE,
    SCRIPT_NODE,
    PATTERN_NODE,
    LAYOUT_NODE,
    GEOMETRY_NODE,
    SIMULATION_ZONE,
    REPEAT_ZONE,
    FOREACH_GEOMETRY_ELEMENT_ZONE,
    _UNKNOWN
};